<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/imagesindex.css" />
    <title>Document</title>
  </head>

  <body>
    <h1>Sunset Image Show Page</h1>
    <ul class="sunsets">
      <li>"<%= image.title %>"</li>
      <img src="<%= image.url%>" alt="" />
      <p>Description:"<%= image.desc %>"</p>
      <p>
        This sunset is from <%= image.location %> and submitted by <%=
        image.User.name%>
      </p>
    </ul>
    <% if (loggedInUser === image.userId) {%>
    <form action="/images/<%= image.id %>?_method=DELETE" method="POST">
      <input type="submit" value="Delete" />
    </form>
    <br />
    <form action="/images/<%= image.id %>/edit" method="GET">
      <input type="submit" value="Edit" />
    </form>
    <%}%>
    <br />
    <a href="/">Take Me Home...</a>
  </body>
</html>
